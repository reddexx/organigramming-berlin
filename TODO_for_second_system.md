TODO for Second System (saved for next session)

Overview:
- This file stores the pending tasks you wanted carried over to your other system so you can continue work tomorrow.

Tasks (priority order):
1) Viewer mode
   - Hide the Info Modal by default in viewer mode.
   - When pressing the global info button in viewer mode, show only the content of `modal-footer`.

2) Admin mode
   - Add a button that provides an iframe/embed snippet (text field with copy button).

3) Organigram adjustments
   - Change dropdown labels to be company-oriented (not Ämter).
   - Ensure custom roles, departments and additional designations (from settings) are prioritized at the top.
   - Hide "Neue Nebenorganisation" entirely in flexible (free) layout.
   - Use the settings list for the top `Zusatzbezeichnung` selector (mb-0 area).
   - Remove `form-check-input` element at the top of `organisation-tab`.
   - Rename "Unterkategorisierende Organisation" to "verlinktes/verknüpftes Organigramm".
   - Rename "URI" to "URL". Make links open external pages when a node is clicked (validate http/https).
   - Remove external Wikidata link option from the UI.
   - Allow node width and height adjustments directly on the node (not inside `organisation-tab`).
   - Show a font preview inside font-family dropdowns.
   - In flexible mode, hide the positioning control (auto/manual) entirely.
   - When a background style option is greyed out, show a hover tooltip explaining it requires a background color to be active.
   - Replace multiple background style checkboxes with a professional single dropdown selector.

4) Misc
   - After implementing changes, run build and tests and push.

Notes:
- I started work on task 1 locally; it is marked "in-progress" in the workspace todo list.
- If you want this file pushed to a different repo (e.g., `LavaSpeak`), tell me which repo/path and I'll create it there as well.

Saved: automatic snapshot for tomorrow's session.
